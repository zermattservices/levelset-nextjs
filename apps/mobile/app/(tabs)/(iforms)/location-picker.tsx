/**
 * Location Picker Modal — Forms tab instance.
 * Re-exports the same picker component used on the home screen
 * so it opens as a modal within the Forms tab stack.
 */

export { default } from "../(home)/location-picker";
