/**
 * Supabase generated types placeholder.
 *
 * Run `pnpm db:gen-types` to generate actual types from the Supabase schema.
 * Until then, the agent service should import types directly.
 */

export {};
