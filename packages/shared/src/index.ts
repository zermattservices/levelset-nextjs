/**
 * @levelset/shared - Shared types and utilities
 *
 * Stub package: re-exports from apps/dashboard/lib for now.
 * Will be fully extracted in a future PR.
 */

// Supabase types will be generated here via `pnpm db:gen-types`
// For now, consumers should import directly from apps/dashboard/lib/supabase.types
export {};
